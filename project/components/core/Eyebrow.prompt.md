Small uppercase, wide-tracked label in Forest Green that introduces every section headline.

```jsx
<Eyebrow>What it does for you</Eyebrow>
```

Default colour is Forest Green. On the Deep Violet section pass `color="rgba(255,255,255,0.7)"`.
