Forest-green check mark with a label. Used for hero trust indicators and the pricing card's inclusion list.

```jsx
<Tick onDark>No lock-in contract</Tick>
```

Pass `onDark` on near-black backgrounds for the lighter label colour.
