Small pill used for captured-data chips in the demo widget, category tags, and urgency flags.

```jsx
<Tag variant="data">Postcode · 3144</Tag>
<Tag variant="urgent">ASAP</Tag>
```

Variants: `data` (mint-on-dark, the demo widget default), `solid` (teal), `outline` (hairline on light), `urgent` (violet).
