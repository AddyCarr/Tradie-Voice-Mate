Surface container for content blocks, problem columns, and the single pricing card.

```jsx
<Card variant="dark" elevated>…</Card>
<Card variant="pricing">…</Card>
```

Variants: `light` (white on mint sections), `dark` (raised ink on near-black), `pricing` (teal-bordered white). Brand is flat — only set `elevated` when a block genuinely needs to lift.
